# AI-Center-Frontend 优化建议清单

> 目标：提升可维护性、稳定性、性能与交付效率。以下按「优先级 + 落地方式」组织，便于迭代执行。

## P0（建议优先处理）

### 1) 补齐基础工程健康度（TypeScript / 路由缺失模块）
**现状**
- `npm run type-check` 当前失败，存在模块缺失、类型声明缺失、路由引用不存在组件等问题。

**风险**
- 影响上线前质量兜底，容易把运行时错误留到生产环境。

**建议**
- 建立「先清零再守护」策略：先修复当前全部 type-check 报错，再将 `type-check` 纳入 CI 必过项。
- 为 `.vue` 文件与第三方依赖补齐类型声明（必要时通过 `env.d.ts` 或 `types/*.d.ts`）。
- 对 `src/router/index.ts` 中不存在的组件路径做一次完整清查（例如管理页/图片页是否缺文件或路径写错）。

---

### 2) 统一鉴权逻辑来源，避免双重跳转与行为分裂
**现状**
- 项目同时在 `src/request.ts`（401 响应拦截）与 `src/access/index.ts`（路由守卫）执行登录态跳转。

**风险**
- 可能出现重复跳转、重定向参数不一致、调试困难。

**建议**
- 明确职责边界：
  - 路由守卫负责页面访问控制；
  - 请求拦截器只做全局错误归一化（例如抛业务错误码）并交给上层处理。
- 提炼 `buildLoginRedirectUrl(currentPath)` 等公共函数，统一重定向拼接规则。

---

### 3) 日志与错误处理治理
**现状**
- 生产代码中保留了 `console.log`（如登录用户信息、请求响应输出）。

**风险**
- 泄露敏感上下文、污染控制台、影响排障效率。

**建议**
- 封装 `logger`（按环境开关：dev 打印、prod 上报）。
- 对请求层增加统一错误提示策略（例如 toast + 可选静默模式）。

## P1（稳定后推进）

### 4) 建立真实可回归的测试分层
**现状**
- 当前测试以字符串 guardrail 检查为主，无法覆盖真实运行行为。

**建议**
- 增加单测：`checkAccess` 权限矩阵（NOT_LOGIN / USER / ADMIN）。
- 增加路由守卫测试：未登录访问受限页应跳转 `/user/login` 并携带 `redirect`。
- 增加 API 层测试：401 与非 2xx 错误分支行为。
- 若条件允许，补一条 E2E（登录 → 访问管理页 → 权限拦截）。

---

### 5) API 层抽象标准化
**建议**
- 统一响应结构类型：`ApiResponse<T>`。
- 对 `API.*` 类型与实际后端响应建立映射层，避免组件直接依赖裸接口字段。
- 将请求配置（timeout、重试、取消请求）集中管理，减少页面内重复逻辑。

---

### 6) 路由配置模块化
**现状**
- `src/router/index.ts` 路由量较大，维护成本上升。

**建议**
- 按业务域拆分路由（`quiz.routes.ts`、`image.routes.ts`、`user.routes.ts`），再在 `index.ts` 汇总。
- 对 `meta` 字段定义类型约束（`access`, `hideInMenu` 等），避免魔法字符串。

## P2（体验与性能优化）

### 7) 首屏性能与按需加载
**建议**
- 对体积较大的页面与图表模块（如 BI）进行更细粒度懒加载。
- 检查静态资源体积（Lottie / 图标），使用压缩与缓存策略。
- 使用 Vite 分析插件观察 chunk 体积，做依赖拆分与去重。

---

### 8) 组件复用与状态管理边界
**建议**
- 抽离列表页通用能力（分页、排序、搜索表单）为 composables。
- 规范 Pinia store：区分持久化状态与临时 UI 状态，减少耦合。

---

### 9) 文档与研发流程
**建议**
- 在 README 增加：项目结构、权限流转图、常见错误排查。
- 增加 `CONTRIBUTING.md`：提交规范、分支规范、测试门禁。
- 引入 PR 模板（变更类型、影响范围、回滚方案、测试证据）。

## 推荐执行顺序（两周示例）
1. **第 1-3 天**：清理 type-check 报错 + 路由引用问题（P0-1）。
2. **第 4-5 天**：统一鉴权职责并收敛重定向工具函数（P0-2）。
3. **第 6-8 天**：补齐权限与路由守卫单测（P1-4）。
4. **第 9-10 天**：路由模块化与 API 类型标准化（P1-5/6）。

---

如果你愿意，我下一步可以直接按这个清单从 **P0 第 1 项** 开始，先把当前 `type-check` 错误分批修复并附上每批提交说明。
